// Static export needs at least one path; the Keystatic UI lives in the layout.
export function generateStaticParams() {
	return [{ params: [] as string[] }]
}

export default function Page() {
	return null
}
